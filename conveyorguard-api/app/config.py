"""중앙 설정 (SSoT).

여러 파일에서 공유하는 설정값·라벨을 한 곳에 둔다.
값이 바뀌면 여기 한 곳만 고치면 된다.
"""

# 상태 라벨 (0~3) — API 응답·전처리·파이프라인 공용 정본
STATE_LABELS = {
    0: "정상",
    1: "경미한 열화",
    2: "중간 열화",
    3: "심각한 열화",
}

# LLM 진단·알림을 트리거하는 심각도 게이트 (이 값 이상이면 진단 수행)
SEVERITY_GATE = 2
